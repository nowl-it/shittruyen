version: "1.0"
profile:
    name: qodana.starter
linter: jetbrains/qodana-js:latest
bootstrap: |
    yarn install
include:
    -   name: Eslint
        paths:
            - "**/*.js"
            - "**/*.jsx"
            - "**/*.ts"
            - "**/*.tsx"